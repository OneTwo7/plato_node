.card.border-dark.mb-3
  .card-header.text-center.text-white.bg-dark Featured Courses
  .card-body
    div(ng-repeat='course in courses | filter:{featured:true} | limitTo:10')
      a(href='/courses/{{ course._id }}') {{ course.title }}
